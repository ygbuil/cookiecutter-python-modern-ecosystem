{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "say_hello",
            "type": "debugpy",
            "request": "launch",
            "justMyCode": false,
            "program": "${workspaceFolder}/.venv/bin/{{ cookiecutter.project_slug | replace("_", "-") }}",
            "args": [
                "say-hello",
                "--name", "Llorenç",
                "--surname", "Buil"
            ],
        }
    ],
}
